nav.navbar.navbar-default.navbar-static-top.custom-nav(role="navigation")
	.navbar-header
		a.navbar-brand(href="#{isBuild ? '#' : '/'}") 首页
	.collapse.navbar-collapse
			ul.nav.navbar-nav.navbar-right
				li
					a(href="https://github.com/hacke2/wooden") by wooden
				li.dropdown
					a.dropdown-toggle(href="#", data-toggle="dropdown", role="button", aria-haspopup="true", aria-expanded="false") 联系我
						span.caret
					ul.dropdown-menu
						each val, key in contact
							li
								-if(key != 'mail')
									a(href="#{val}") #{key}
								-else
									a(href="mailto:#{val}") #{key}
								

